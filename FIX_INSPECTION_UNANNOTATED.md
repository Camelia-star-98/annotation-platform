# 🐛 修复：抽样质检抽到未标注数据的问题

## 📋 问题描述

**现象：** 在"抽样质检"页面，质检员会看到**未标注的数据**（`human_annotated_text` 为空或 null）。

**影响：** 
- ❌ 质检员看到空白的标注内容
- ❌ 无法正常进行质检工作
- ❌ 显示"未标注"字样的数据

## 🔍 问题根源

### 位置
文件：`src/api/database.ts`  
函数：`getPendingInspectionAnnotations` （第408-469行）

### 原因
该函数在查询待质检数据时，**没有过滤未标注的句子**，导致返回了所有数据：

```typescript
// ❌ 旧代码 - 会返回所有句子（包括未标注的）
let query = supabase
  .from('annotations')
  .select('...')
  .eq('video_id', videoId)
  // 没有过滤条件！
  .order('sentence_no', { ascending: true });
```

注释中明确写着："质检所有句子（包括未标注和已标注的）"。

## ✅ 解决方案

### 修改内容

添加过滤条件，**只查询已标注的数据**：

```typescript
// ✅ 新代码 - 只返回已标注的句子
let query = supabase
  .from('annotations')
  .select('...')
  .eq('video_id', videoId)
  // ✅ 只查询已标注的数据（有人工标注内容的）
  .not('human_annotated_text', 'is', null)
  .neq('human_annotated_text', '')
  .order('sentence_no', { ascending: true });
```

### 过滤逻辑

| 条件 | 说明 | 作用 |
|------|------|------|
| `.not('human_annotated_text', 'is', null)` | 排除 null 值 | 过滤未初始化的数据 |
| `.neq('human_annotated_text', '')` | 排除空字符串 | 过滤已清空的数据 |

## 📊 修改影响

### ✅ 修改后的行为

| 场景 | 修改前 | 修改后 |
|------|--------|--------|
| **未标注的句子** | ❌ 会显示 | ✅ 不显示 |
| **已标注的句子** | ✅ 显示 | ✅ 显示 |
| **质检通过的句子** | ✅ 显示 | ✅ 显示 |
| **质检不通过的句子** | ✅ 显示 | ✅ 显示 |

### 受影响的页面

1. **抽样质检页面** (`InspectionManagePage.tsx`)
   - 调用 `getPendingInspectionAnnotations` 获取数据
   - 只显示已标注的数据

2. **质检选择页面** (`InspectionSelectPage.tsx`)
   - 显示"待质检"数量时，只统计已标注的数据
   - 抽样时只从已标注的数据中抽取

## 🧪 验证方法

### 测试步骤

1. **准备测试数据**
   - 创建一个视频
   - 标注部分句子（如：只标注第1、3、5句）
   - 保留部分句子未标注（如：第2、4、6句）

2. **进入抽样质检**
   - 主页 → "抽样质检"
   - 选择刚才的视频
   - 点击"开始质检"

3. **验证结果**
   - ✅ **应该只看到：** 第1、3、5句（已标注的）
   - ✅ **不应该看到：** 第2、4、6句（未标注的）

### 预期行为

```
✅ 正确：只显示已标注的句子
句子1: [已标注内容] → 可以质检
句子3: [已标注内容] → 可以质检
句子5: [已标注内容] → 可以质检

❌ 错误（修复前）：显示所有句子
句子1: [已标注内容] → 可以质检
句子2: [未标注] → 无法质检 ⚠️
句子3: [已标注内容] → 可以质检
句子4: [未标注] → 无法质检 ⚠️
```

## 📝 相关文件

| 文件 | 说明 | 修改情况 |
|------|------|----------|
| `src/api/database.ts` | 数据库查询函数 | ✅ 已修改 |
| `src/pages/InspectionManagePage.tsx` | 抽样质检页面 | 无需修改 |
| `src/pages/InspectionSelectPage.tsx` | 质检选择页面 | 无需修改 |

## 🎯 业务逻辑说明

### 质检流程

```
1. 标注员标注
   ↓ (human_annotated_text 有内容)
2. 数据进入"待质检"池
   ↓ (is_qualified = null, inspector = null)
3. 质检员抽样质检
   ↓ (只抽取已标注的数据)
4. 质检员标记通过/不通过
   ↓ (is_qualified = true/false, inspector = 质检员姓名)
5. 质检完成
```

### 为什么不质检未标注的数据？

| 理由 | 说明 |
|------|------|
| **业务逻辑** | 未标注的数据没有内容可以质检 |
| **工作效率** | 质检员无需浪费时间在空白数据上 |
| **数据完整性** | 未标注的数据应该在标注阶段完成 |

## 🚀 后续建议

### 1. 优化标注流程

建议在"标注任务列表"页面添加提示：
- 显示"未完成标注"的句子数量
- 提醒标注员完成所有句子的标注

### 2. 添加数据完整性检查

在发布任务前检查：
```typescript
// 检查是否所有句子都已标注
const hasUnAnnotated = annotations.some(a => 
  !a.human_annotated_text || a.human_annotated_text.trim() === ''
);

if (hasUnAnnotated) {
  alert('请先完成所有句子的标注！');
}
```

### 3. 统计信息更准确

在统计页面区分：
- 📊 总句子数
- ✅ 已标注句子数
- ❌ 未标注句子数

## ⏱️ 修复时间

- **发现时间：** 2025年12月4日
- **修复时间：** 2025年12月4日
- **修复版本：** 当前版本

## 👤 修复记录

- **问题报告：** 用户反馈
- **问题分析：** AI 助手
- **代码修复：** AI 助手
- **测试验证：** 待用户验证

---

**状态：** ✅ 已修复  
**需要重启：** 是（需要重启开发服务器使修改生效）

