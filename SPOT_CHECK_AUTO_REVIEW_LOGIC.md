# 抽检自动进入复检逻辑说明

## 📋 需求背景

实现抽检逻辑：**只要抽检的数据全部通过质检（没有质检不通过的），视频就应该自动从质检列表移除，进入复检流程。**

## 🎯 业务规则

### 视频从质检列表移除的条件：

1. ✅ **有质检通过的数据**（`passedCount > 0`）
2. ✅ **没有质检不通过的数据**（`failedCount = 0`）
3. ℹ️ **即使还有待质检数据**（`pendingCount > 0`），也应该进入复检

### 示例说明

#### 示例1：应该进入复检 ✅
```
视频：第七批第一次改写_数学-01.xlsx
- 总标注数：127条
- 质检通过：102条 ✅
- 质检不通过：0条 ✅
- 待质检：25条

结果：从质检列表移除，进入复检流程
原因：抽检的102条数据全部通过，没有不通过的数据
```

#### 示例2：继续留在质检列表 ❌
```
视频：第五批第一轮-语文-1.mp4
- 总标注数：100条
- 质检通过：50条
- 质检不通过：25条 ❌
- 待质检：25条

结果：继续显示在质检列表
原因：有25条质检不通过的数据，需要标注员修改
```

#### 示例3：继续留在质检列表 ❌
```
视频：测试视频-数学.mp4
- 总标注数：50条
- 质检通过：0条 ❌
- 质检不通过：0条
- 待质检：50条

结果：继续显示在质检列表
原因：还没有任何质检通过的数据
```

## 🔧 技术实现

### 修改文件
- `src/pages/InspectionSelectPage.tsx`（第172-177行）

### 核心代码

```typescript
// 🆕 抽检逻辑：如果视频有质检通过的数据且没有质检不通过的数据，则不显示在质检列表
// 即使还有待质检数据，只要抽检的数据全部通过，就应该进入复检流程
if (passedCount > 0 && failedCount === 0) {
  console.log(`⏭️ 跳过视频 ${video.name}：抽检数据全部通过（${passedCount}条通过，0条不通过），应进入复检流程`);
  continue; // 跳过这个视频，不显示在质检列表中
}
```

## 📊 测试验证

### 测试SQL脚本
运行 `TEST_inspection_filter_logic.sql` 可以验证筛选逻辑：

```bash
# 在Supabase SQL Editor中运行
TEST_inspection_filter_logic.sql
```

该脚本会输出：
1. 所有视频的质检统计和是否应该显示在质检列表
2. 应该进入复检的视频数量统计
3. 具体哪些视频应该进入复检

### 预期结果

运行测试脚本后，应该看到：
- ✅ 有质检通过且无质检不通过的视频被标记为"不显示（进入复检）"
- ✅ 有质检不通过的视频被标记为"显示在质检列表"
- ✅ 没有质检通过数据的视频被标记为"显示在质检列表"

## 🎯 用户体验

### 质检员角度
1. 打开质检页面，只看到需要质检或需要重新质检的视频
2. 抽检通过的视频自动消失，减少干扰
3. 提高工作效率，专注于需要质检的内容

### 复检员角度
1. 打开复检页面，能看到所有抽检通过的视频
2. 可以开始对这些视频进行复检
3. 复检完成后，视频从复检列表移除

## 📝 日志输出

修改后会在浏览器控制台输出详细日志：

```
⏭️ 跳过视频 第七批第一次改写_数学-01.xlsx：抽检数据全部通过（102条通过，0条不通过），应进入复检流程
📊 视频 第五批第一轮-语文-1.mp4 统计：总100条，通过50条，不通过25条，待质检25条
```

## 🚀 部署

### 前端部署
```bash
# 构建生产版本
npm run build

# 或直接在开发环境测试
npm run dev
```

### Git提交
已提交到本地仓库：
```
commit 4299a8e
实现抽检逻辑：质检通过且无不通过数据的视频自动进入复检流程
```

## ⚠️ 注意事项

1. **不影响现有数据**：只改变了视频的显示逻辑，不修改数据库数据
2. **可逆操作**：如果需要回退，可以git revert
3. **性能影响**：计算逻辑在前端进行，不增加数据库查询负担
4. **兼容性**：与现有的复检逻辑完全兼容

## 📚 相关文档

- `INSPECTION_DATA_FILTERING.md` - 质检页面数据过滤说明
- `SPOT_CHECK_AUTO_PASS_GUIDE.md` - 抽检自动通过指南
- `SPOT_CHECK_COMPLETION_REPORT.md` - 抽检完成报告

---

**最后更新时间**：2025-12-11  
**版本**：v1.0

