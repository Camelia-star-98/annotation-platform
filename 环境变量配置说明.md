# 🔐 环境变量配置模板

**⚠️ 本文档包含如何配置项目环境变量的说明，但不包含实际的敏感信息。**

---

## 📋 配置步骤

### 1. 创建配置文件

在项目根目录创建 `.env.local` 文件：

```bash
# 在项目根目录（annotation-platform/）下
touch .env.local
```

### 2. 填写配置内容

将以下内容复制到 `.env.local` 文件中：

```env
# Supabase 后端配置
VITE_SUPABASE_URL=你的Supabase项目URL
VITE_SUPABASE_ANON_KEY=你的Supabase匿名密钥
```

### 3. 获取实际配置值

**项目负责人会通过私密方式（加密邮件/私聊）单独发送给你实际的配置值。**

配置内容示例（实际值需替换）：

```env
# Supabase 后端配置
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey...（很长的字符串）
```

### 4. 验证配置

配置完成后，启动项目：

```bash
npm run dev
```

如果能正常访问并连接到数据库，说明配置成功！

---

## 🔍 配置项说明

### VITE_SUPABASE_URL

- **作用**: Supabase 项目的 API 地址
- **格式**: `https://xxxxx.supabase.co`
- **获取方式**: Supabase 控制台 → Settings → API → Project URL
- **示例**: `https://abcdefghijklmn.supabase.co`

### VITE_SUPABASE_ANON_KEY

- **作用**: Supabase 匿名访问密钥（客户端使用）
- **格式**: 长字符串（JWT Token）
- **获取方式**: Supabase 控制台 → Settings → API → Project API keys → anon public
- **示例**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

---

## 🔒 安全须知

### ⚠️ 重要安全规则

1. **不要提交到 Git**
   - `.env.local` 文件已在 `.gitignore` 中
   - 永远不要把这个文件添加到 Git
   - 不要截图或拍照分享

2. **不要公开分享**
   - 不要发到公开的聊天群
   - 不要上传到云盘分享链接
   - 不要复制到公开的文档

3. **如果不小心泄露**
   - 立即通知项目负责人
   - 需要重新生成密钥
   - 团队所有成员需要更新配置

### ✅ 正确的分享方式

如果需要给新成员配置：
- ✅ 加密的私人消息
- ✅ 加密邮件
- ✅ 密码保护的文件
- ✅ 面对面交接

### ❌ 错误的分享方式

- ❌ 公开的聊天群
- ❌ 未加密的邮件
- ❌ GitHub Issue/PR
- ❌ 项目文档中直接写明

---

## 🛠️ 常见问题

### Q1: 为什么要用 `.env.local` 而不是 `.env`?

- `.env.local` 是本地个人配置，不会被提交
- `.env` 可能会被其他开发者提交（包含示例值）
- Vite 优先读取 `.env.local`

### Q2: 配置文件放在哪里？

```
annotation-platform/
├── .env.local          ← 在这里（项目根目录）
├── src/
├── public/
├── package.json
└── README.md
```

### Q3: 配置后还是连接不上？

检查清单：
- [ ] 文件名是否正确：`.env.local`（注意前面有个点）
- [ ] 文件位置是否正确：项目根目录
- [ ] 配置内容是否正确：复制时没有多余空格
- [ ] 是否重启了开发服务器：`Ctrl+C` 然后重新 `npm run dev`

### Q4: 如何验证配置是否生效？

在代码中添加调试：

```typescript
// 在 src/api/supabase.ts 或任何文件中
console.log('Supabase URL:', import.meta.env.VITE_SUPABASE_URL);

// 如果打印出完整 URL 说明配置生效
// 如果是 undefined 说明配置未生效
```

### Q5: 团队共享配置怎么办？

**不要共享实际密钥！** 每个环境应该用独立的配置。

推荐做法：
1. 开发环境：每个开发者使用同一个开发环境的 Supabase 项目
2. 生产环境：使用独立的生产环境配置（通过 CI/CD 注入）

---

## 📚 环境变量最佳实践

### 1. 命名规范

Vite 项目中，客户端可访问的环境变量必须以 `VITE_` 开头：

```env
# ✅ 正确 - 客户端可访问
VITE_SUPABASE_URL=xxx
VITE_API_KEY=xxx

# ❌ 错误 - 客户端无法访问
SUPABASE_URL=xxx
API_KEY=xxx
```

### 2. 敏感信息分级

| 级别 | 类型 | 可以提交到Git? | 示例 |
|------|------|---------------|------|
| 公开 | 可公开的配置 | ✅ 可以（在 .env.example） | API端点地址 |
| 限制 | 团队内部可见 | ❌ 不可以 | 开发环境密钥 |
| 机密 | 生产环境密钥 | ❌ 绝对不可以 | 生产数据库密码 |

### 3. 提供配置模板

在项目中创建 `.env.example`（可以提交到Git）：

```env
# .env.example - 配置模板（不含真实值）

# Supabase 后端配置
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# 配置说明：
# 1. 复制此文件为 .env.local
# 2. 填写实际的配置值
# 3. 不要提交 .env.local 到 Git
```

---

## 🔐 如何为新成员配置环境

### 给项目负责人的清单

当新成员加入时：

1. **准备配置内容**
   ```env
   VITE_SUPABASE_URL=https://xxxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGci...
   ```

2. **选择安全的分享方式**
   - 加密的即时通讯（推荐：微信私聊、企业微信）
   - PGP 加密邮件
   - 密码保护的文档（密码另外通知）

3. **发送配置和说明**
   ```
   嗨 @新成员，

   项目环境配置如下，请妥善保管，不要公开分享：

   VITE_SUPABASE_URL=https://xxxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGci...（完整密钥）

   配置步骤：
   1. 在项目根目录创建 .env.local 文件
   2. 复制上面的内容到文件中
   3. 保存后重启开发服务器

   详细说明见：docs/环境变量配置.md

   有问题随时联系我！
   ```

4. **确认配置成功**
   - 询问是否启动成功
   - 解答配置问题
   - 确认能正常连接数据库

---

## 📖 延伸阅读

- [Vite 环境变量文档](https://vitejs.dev/guide/env-and-mode.html)
- [Supabase 快速开始](https://supabase.com/docs/guides/getting-started)
- [如何保护敏感信息](https://docs.github.com/en/actions/security-guides/encrypted-secrets)

---

## 🆘 遇到问题？

如果配置过程中遇到问题：

1. 检查本文档的"常见问题"部分
2. 查看 [SUPABASE_GUIDE.md](./SUPABASE_GUIDE.md)
3. 联系项目负责人
4. 在团队内部讨论（不要暴露实际密钥）

---

**记住三个原则：**
1. 🔒 保护好密钥，不要泄露
2. 📝 配置正确，确保能用
3. 🆘 有问题及时求助

祝配置顺利！🚀

