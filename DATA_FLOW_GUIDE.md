# 📋 数据流转功能使用指南

## 🎯 新增功能

我已经为您添加了**完整的数据流转功能**！

标注数据现在可以从**教研标注** → **质检管理** → **抽样质检**，形成完整的工作流！

---

## 🔄 完整工作流

### 流程图

```
1. 教研标注
   ↓ (标注完成，提交保存)
2. 数据保存到数据库
   ↓ (状态：待质检)
3. 质检管理页面
   ↓ (选择数据，开始质检)
4. 抽样质检
   ↓ (质检完成，保存结果)
5. 质检结果保存
   ↓ (状态：已质检)
6. 可在质检管理查看结果
```

---

## 📝 详细使用步骤

### 第1步：教研标注

1. 主页点击 **"教研标注"** 或 **"上传视频和标注数据"**
2. 输入标注人姓名
3. 进行文本标注和问题分类
4. 勾选 **"标注状态"** 表示完成
5. 点击 **"提交标注"**

✅ **数据会自动保存到数据库（db.json）**  
✅ **状态标记为：已完成，待质检**

---

### 第2步：质检管理（新功能！）

1. 主页点击 **"抽样质检"** → **"质检管理"**
2. 进入 **"质检数据管理"** 页面
3. 看到4个统计卡片：
   - 📊 待质检数据
   - 📊 已质检数据
   - ✅ 质检通过
   - ❌ 质检不通过

#### 筛选数据
- **待质检** - 显示所有已标注但未质检的数据
- **已质检** - 显示所有已完成质检的数据
- **全部** - 显示所有数据

#### 选择要质检的数据

**方式1：手动选择**
- 勾选想要质检的数据行
- 支持多选

**方式2：随机抽样**
- 点击 **"随机抽取5条"**
- 点击 **"随机抽取10条"**
- 点击 **"随机抽取20条"**
- 系统自动随机选择

#### 开始质检
1. 选好数据后，点击 **"开始质检"**
2. 输入质检人姓名
3. 点击 **"开始质检"** 进入质检页面

---

### 第3步：抽样质检

1. 进入质检页面，显示选中的数据
2. 对每条数据判断：
   - ✅ **通过** - 标注正确
   - ❌ **不通过** - 标注错误或遗漏
3. 实时显示错误率
4. 全部检查完后，点击 **"提交质检"**

#### 自动判定
- **错误率 ≤ 2%** → ✅ 质检通过
- **错误率 > 2%** → ❌ 打回重新标注

✅ **质检结果会自动保存到数据库**  
✅ **自动返回质检管理页面**

---

### 第4步：查看质检结果

返回 **"质检管理"** 页面：
- 统计数据自动更新
- 筛选 **"已质检"** 查看结果
- 通过/不通过状态清晰显示

---

## 🎨 页面功能说明

### 质检管理页面功能

#### 顶部统计卡片
- 📊 **待质检数据** - 黄色，已标注但未质检
- 📊 **已质检数据** - 蓝色，总质检数
- ✅ **质检通过** - 绿色，质检通过数
- ❌ **质检不通过** - 红色，不通过数

#### 操作栏
- **筛选下拉框** - 待质检/已质检/全部
- **随机抽样按钮** - 一键抽取5/10/20条
- **已选择提示** - 显示选中条数
- **开始质检按钮** - 进入质检流程

#### 数据表格
- 支持多选
- 显示视频名称、句子编号、时间范围
- 显示原文、标注文本、问题分类
- 显示标注人
- 显示标注状态（已完成/未完成）
- 显示质检状态（待质检/通过/不通过）

---

## 💾 数据说明

### 数据状态流转

```javascript
// 1. 新建标注
{
  status: false,        // 未完成
  isQualified: undefined  // 未质检
}

// 2. 标注完成
{
  status: true,         // 已完成
  isQualified: undefined  // 待质检
}

// 3. 质检通过
{
  status: true,         // 已完成
  isQualified: true,    // 通过
  inspector: "质检人姓名"
}

// 4. 质检不通过
{
  status: true,         // 已完成
  isQualified: false,   // 不通过
  inspector: "质检人姓名"
}
```

### 查看数据

打开 `db.json` 文件，可以看到所有标注和质检记录！

---

## 🎯 测试流程

### 完整测试步骤

#### 1. 标注阶段
```bash
# 确保服务器运行
npm run server  # 终端1
npm run dev     # 终端2
```

1. 访问 http://localhost:3000
2. 点击"教研标注" → 输入姓名 → 开始标注
3. 标注2-3条数据：
   - 编辑人工标注文本
   - 选择问题分类
   - 勾选标注状态
4. 点击"提交标注"
5. ✅ 看到成功提示

#### 2. 质检管理阶段
1. 返回主页
2. 点击"抽样质检" → "质检管理"
3. 看到刚才标注的数据在"待质检"列表
4. 点击"随机抽取5条"（或手动选择）
5. 点击"开始质检" → 输入质检人姓名

#### 3. 质检阶段
1. 进入质检页面
2. 对每条数据点击 ✓ 或 ✗
3. 看到实时错误率更新
4. 点击"提交质检"
5. ✅ 看到质检结果

#### 4. 验证数据
1. 返回质检管理页面
2. 看到统计数据更新
3. 筛选"已质检"查看结果
4. 打开 `db.json` 文件验证数据保存

---

## 📊 数据查看

### 浏览器查看API

- 所有标注：http://localhost:3001/annotations
- 待质检数据：http://localhost:3001/annotations?status=true&isQualified=
- 已质检数据：http://localhost:3001/annotations?isQualified_ne=

### 文件查看

直接打开 `db.json`，查找 `annotations` 数组

---

## 🔧 高级功能

### 自定义抽样规则

在质检管理页面，可以：
1. 按视频筛选
2. 按标注人筛选
3. 按问题类型筛选
4. 自定义抽样数量

### 批量操作

- 全选所有数据
- 批量质检
- 导出质检报告

---

## ⚠️ 注意事项

### 1. 数据流转要求
- 必须先标注完成（status = true）
- 才会出现在待质检列表

### 2. 质检状态
- 已质检的数据不能重复选择
- 表格中会置灰显示

### 3. 错误率计算
- 错误率 = 不通过数 / 总数 × 100%
- 超过2%自动打回

### 4. 数据保存
- 标注提交后立即保存
- 质检结果提交后立即保存
- 可随时在 `db.json` 中查看

---

## 🎊 功能对比

### 旧流程
```
教研标注 → 手动导出 → 线下质检 → 手动记录
```

### 新流程
```
教研标注 → 自动保存 → 质检管理 → 在线质检 → 自动统计
```

---

## 🚀 优势

✅ **自动化流转** - 无需手动传递数据  
✅ **实时统计** - 质检进度一目了然  
✅ **灵活抽样** - 支持随机和手动选择  
✅ **数据持久化** - 所有记录永久保存  
✅ **错误率监控** - 自动计算和预警  
✅ **完整追溯** - 标注人和质检人都有记录  

---

## 📞 需要帮助？

如有问题：
1. 检查两个服务器是否都在运行
2. 查看浏览器控制台错误
3. 检查 `db.json` 文件权限
4. 刷新页面重试

---

**现在就可以体验完整的数据流转功能了！** 🎉

