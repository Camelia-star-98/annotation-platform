# 🚀 启动说明

## 问题诊断

如果应用打不开，通常是以下原因：
1. **JSON Server 后端未启动**（端口 3001）
2. **前端开发服务器未启动**（端口 5173）
3. **依赖未安装**

## ✅ 解决步骤

### 步骤 1：安装依赖

```bash
cd /Users/ailian/Downloads/annotation-platform
npm install
```

### 步骤 2：安装 json-server（如果未安装）

```bash
npm install json-server --save-dev
```

### 步骤 3：启动服务

**方法 A：使用两个终端窗口（推荐）**

**终端 1 - 启动后端：**
```bash
cd /Users/ailian/Downloads/annotation-platform
npm run server
```

看到以下输出表示成功：
```
  \{^_^}/ hi!
  Loading db.json
  Done
  
  Resources
  http://localhost:3001/videos
  http://localhost:3001/annotations
  
  Home
  http://localhost:3001
```

**终端 2 - 启动前端：**
```bash
cd /Users/ailian/Downloads/annotation-platform
npm run dev
```

看到以下输出表示成功：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**方法 B：使用一键启动脚本**

```bash
cd /Users/ailian/Downloads/annotation-platform
./start.sh
```

## 🌐 访问应用

启动成功后，在浏览器中访问：
- **前端应用**：http://localhost:5173
- **后端 API**：http://localhost:3001

## 🔍 验证服务是否运行

打开新的终端窗口，运行：

```bash
# 检查后端
curl http://localhost:3001/videos

# 检查端口占用
lsof -i :3001
lsof -i :5173
```

## ⚠️ 常见问题

### 问题 1：端口被占用

如果端口 3001 或 5173 已被占用：

```bash
# 查找占用端口的进程
lsof -i :3001
lsof -i :5173

# 杀死进程（替换 PID 为实际进程号）
kill -9 <PID>
```

### 问题 2：json-server 命令未找到

```bash
# 安装 json-server
npm install json-server --save-dev

# 或使用 npx
npx json-server --watch db.json --port 3001
```

### 问题 3：依赖安装失败

```bash
# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm install
```

## 📞 需要帮助？

如果以上方法都不行，请检查：
1. Node.js 是否已安装：`node --version`
2. npm 是否可用：`npm --version`
3. 浏览器控制台是否有错误信息（按 F12）

