# 质检页面数据量说明

## 问题描述

用户发现在"质检数据管理"页面，某些视频显示的"总标注数"比预期要少。例如：
- **"第5批第一轮-语文-1.mp4"** 只显示 **4条**
- **"第五批第一轮-语文-1（二次）"** 只显示 **10条**

## 原因分析

这是**正常现象**，并非数据丢失。质检视频选择页面（`InspectionSelectPage.tsx`）有特定的数据过滤逻辑。

### 数据过滤规则

页面只显示满足以下**所有条件**的数据：

1. ✅ **有人工标注内容**（`human_annotated_text` 不为 null 且不为空字符串）
2. ✅ **未完成复检**（`review_status` 为 null）

### 代码位置

文件：`src/pages/InspectionSelectPage.tsx`

关键代码（第87-93行）：

```typescript
const { data: allAnnotations, error: annotationsError } = await supabase
  .from('annotations')
  .select('id, video_id, sentence_no, annotator, human_annotated_text, inspector, is_qualified, review_status')
  .in('video_id', videoIds)
  .not('human_annotated_text', 'is', null)
  .neq('human_annotated_text', '')
  .is('review_status', null);  // 排除已复检完成的数据
```

## 被过滤掉的数据

以下类型的数据**不会**在质检视频选择页面显示：

| 数据类型 | 原因 | 说明 |
|---------|------|------|
| ❌ 无标注内容 | `human_annotated_text` 为空 | 空白句子、未标注的数据 |
| ❌ 已复检完成 | `review_status` 不为 null | 复检员已经复检过的数据 |

## 数据统计含义

| 字段 | 含义 | 计算方式 |
|-----|------|---------|
| **总标注数** | 可供质检的数据总量 | 有标注内容 + 未完成复检 |
| **待质检** | 还没有质检员检查的数据 | 无 inspector 或 is_qualified 为 null |
| **质检通过** | 质检员标记为通过的数据 | is_qualified = true |
| **质检不通过** | 质检员标记为不通过的数据 | is_qualified = false |
| **已复检完成** | 复检员已经复检过的数据 | review_status 不为 null |

## 查看完整数据

如果需要查看**所有数据**（包括已质检、已复检的），可以：

1. 访问 **"诊断页面"**（`/diagnose`）- 无过滤，显示所有数据
2. 访问 **"质检数据管理页面"**（`/inspection-manage`）- 可以查看当前质检的所有数据
3. 访问 **"数据库"** - 直接查询 `annotations` 表

## 解决方案

### ✅ 已实施的改进

1. **添加提示图标**：在"总标注数"列标题旁边添加了问号图标，鼠标悬停显示过滤规则
2. **添加说明卡片**：在页面顶部添加了蓝色说明卡片，明确显示数据过滤规则
3. **更新界面提示**：使用颜色和符号清晰标注哪些数据被包含、哪些被排除

### 📝 用户提示

当用户看到"总标注数"时，应该理解：

- 这是**待质检的数据量**，不是视频的全部数据
- 已经质检过的数据、已复检完成的数据不会重复显示
- 这样设计是为了让质检员专注于**尚未处理的数据**

## 数据流转示意图

```
原始数据（annotations表）
    │
    ├─ 有标注内容 ✅
    │   ├─ 未复检 ✅
    │   │   ├─ 未质检 → 【显示在质检列表】⭐
    │   │   └─ 已质检 → 【显示在质检列表】
    │   └─ 已复检 ❌ → 不显示
    │
    └─ 无标注内容 ❌ → 不显示
```

## 数据示例

假设一个视频有 **100条** annotations：

- 50条 有标注内容
  - 40条 未复检
    - 10条 待质检 ✅ **显示**
    - 30条 已质检 ✅ **显示**
  - 10条 已复检 ❌ **不显示**
- 50条 无标注内容 ❌ **不显示**

**质检页面显示：40条**（总标注数）
- 待质检：10条
- 已质检：30条

## 总结

✅ **数据没有丢失，只是被过滤了！**

质检视频选择页面的设计目的是让质检员看到**需要处理或正在处理的数据**，自动过滤掉不需要质检的数据（无标注内容）和已经完成后续流程的数据（已复检完成）。

这样的设计使得：
1. 质检员可以专注于当前需要处理的数据
2. 避免重复质检已经通过复检流程的数据
3. 提高工作效率

---

**更新时间**：2025年11月28日
**相关文件**：
- `src/pages/InspectionSelectPage.tsx` - 质检视频选择页面
- `src/pages/InspectionManagePage.tsx` - 质检管理页面

