# 🚀 自动部署指南

## 快速使用

### 方法 1：使用部署脚本（推荐）

```bash
# 一键部署（会提交所有修改并推送）
./deploy.sh "修复数据统计分页问题"

# 或者使用默认提交说明
./deploy.sh
```

### 方法 2：手动操作

```bash
# 1. 添加修改
git add .

# 2. 提交修改
git commit -m "你的提交说明"

# 3. 推送到远程
git push
```

## 自动部署流程

```
修改代码 → 运行 ./deploy.sh → Git 推送 → Vercel 自动部署 → 线上生效
```

**时间**：推送后 1-2 分钟内，线上环境会自动更新

## 注意事项

1. **首次使用前确保**：
   - 已连接 Git 远程仓库（GitHub/GitLab）
   - 已在 Vercel 中连接该仓库
   
2. **Vercel 会自动监听**：
   - 每次推送到 `main` 分支会自动部署到生产环境
   - 推送到其他分支会创建预览环境

3. **环境变量**：
   - 确保 Vercel 中已配置 Supabase 相关环境变量：
     - `VITE_SUPABASE_URL`
     - `VITE_SUPABASE_ANON_KEY`

## 查看部署状态

访问 Vercel Dashboard：https://vercel.com/dashboard

可以看到：
- 部署进度
- 部署日志
- 线上网址
- 预览链接

## 常见问题

**Q: 推送后多久生效？**
A: 通常 1-2 分钟，Vercel 会自动构建并部署

**Q: 如何回滚到之前的版本？**
A: 在 Vercel Dashboard 中选择之前的部署记录，点击 "Promote to Production"

**Q: 本地开发时如何测试？**
A: 运行 `npm run dev`，访问 `http://localhost:5173`

