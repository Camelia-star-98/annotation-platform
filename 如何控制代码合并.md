# 🔒 如何控制代码合并到主分支

本文档说明如何通过 GitHub 的分支保护规则，确保代码质量和团队协作流程。

---

## 📋 目录

- [为什么需要分支保护](#为什么需要分支保护)
- [已配置的保护规则](#已配置的保护规则)
- [实际工作流程](#实际工作流程)
- [审核者指南](#审核者指南)
- [常见场景处理](#常见场景处理)

---

## 🎯 为什么需要分支保护

### 没有分支保护的问题

```bash
# ❌ 任何人都可以直接推送到 main
git push origin main  # 危险！未经审核的代码进入生产环境

# 可能导致：
# - 引入 bug 影响其他人
# - 覆盖别人的代码
# - 破坏生产环境
# - 无法追溯问题
```

### 有了分支保护的好处

```bash
# ✅ 必须通过 PR 流程
git push origin feature/my-feature
# 然后创建 PR → 代码审核 → 讨论改进 → 合并

# 带来的好处：
# ✅ 代码质量保证（至少两双眼睛看过）
# ✅ 知识共享（团队了解彼此的改动）
# ✅ 防止意外破坏
# ✅ 保持主分支稳定
```

---

## 🛡️ 已配置的保护规则

我们为 `main` 分支配置了以下保护规则：

### 1. 禁止直接推送

**规则：** 任何人都不能直接 push 到 main 分支

```bash
# ❌ 这样会失败
git checkout main
git commit -m "fix something"
git push origin main
# Error: Protected branch update failed

# ✅ 正确做法
git checkout -b fix/something
git commit -m "fix something"
git push origin fix/something
# 然后在 GitHub 上创建 PR
```

### 2. 必须通过 Pull Request

**规则：** 代码只能通过 Pull Request 合并到 main

```
开发者分支 → Pull Request → 代码审核 → 合并到 main
```

### 3. 至少需要 1 人审核通过

**规则：** PR 必须至少有 1 个团队成员批准才能合并

```
PR 创建 → 请求审核 → 审核者检查代码 → 批准 → 可以合并
```

### 4. 审核后如有新提交需重新审核

**规则：** 如果 PR 被审核后又推送了新代码，需要重新审核

```
PR 审核通过 → 发现问题修改代码 → 推送新提交 → 需要重新审核
```

这确保审核者看到的是最终会被合并的代码。

---

## 🔄 实际工作流程

### 开发者视角

#### 第 1 步：创建功能分支

```bash
# 从最新的 main 分支创建
git checkout main
git pull origin main
git checkout -b feature/add-export-function
```

#### 第 2 步：开发并提交

```bash
# 开发功能...
git add .
git commit -m "feat: 添加导出功能"

# 推送到远程
git push origin feature/add-export-function
```

#### 第 3 步：创建 Pull Request

1. 访问 GitHub 仓库
2. 点击 "Compare & pull request"
3. 填写 PR 描述（说明做了什么，为什么这么做）
4. 选择审核者（可选，系统会自动通知团队）
5. 点击 "Create pull request"

#### 第 4 步：等待审核

PR 创建后会显示：

```
❌ Review required
   At least 1 approving review is required
   
⚠️ Merging is blocked
   Pull request must be approved before merging
```

#### 第 5 步：响应审核意见

审核者可能会：
- ✅ **批准（Approve）** - 太好了！可以合并了
- 💬 **评论（Comment）** - 提出建议，不阻止合并
- ❌ **请求修改（Request changes）** - 必须修改才能合并

如果需要修改：
```bash
# 在原分支上修改
git add .
git commit -m "fix: 根据审核意见修改"
git push origin feature/add-export-function
# PR 会自动更新，需要重新审核
```

#### 第 6 步：合并代码

审核通过后会显示：

```
✅ All checks have passed
✅ 1 approving review
✅ This branch has no conflicts with the base branch

[Merge pull request] 按钮变绿，可以合并了！
```

点击合并按钮，选择合并方式：
- **Merge commit** - 保留所有提交历史（推荐）
- **Squash and merge** - 合并为一个提交
- **Rebase and merge** - 线性历史

#### 第 7 步：清理分支

```bash
# 删除本地分支
git checkout main
git pull origin main
git branch -d feature/add-export-function

# GitHub 上可以选择自动删除已合并的分支
```

---

### 审核者视角

#### 收到审核请求

你会收到通知：
- GitHub 邮件通知
- GitHub 网站通知
- 团队聊天通知（如果配置了）

#### 审核流程

##### 1. 查看 PR 描述

了解：
- 这个 PR 做了什么？
- 为什么要这么做？
- 如何测试？

##### 2. 查看改动的代码

点击 "Files changed" 标签，审查代码：

```diff
+ const handleExport = async () => {
+   const data = await fetchData();
+   exportToExcel(data);
+ };
```

##### 3. 添加评论或建议

可以：
- 点击代码行号添加行内评论
- 提出改进建议
- 询问设计决策

示例评论：
```
💡 建议：这里是否需要添加错误处理？
🤔 疑问：为什么选择这个方案而不是 XXX？
👍 优点：错误处理很完善！
⚠️ 问题：这里可能会导致内存泄漏
```

##### 4. 提交审核结果

- ✅ **Approve** - 代码没问题，可以合并
- 💬 **Comment** - 提出建议但不阻止合并
- ❌ **Request changes** - 必须修改才能合并

##### 5. 跟进修改

如果请求了修改：
- 关注开发者的更新
- 修改完成后重新审核
- 确认问题解决后批准

---

## 📚 常见场景处理

### 场景 1: 紧急修复如何快速合并？

即使是紧急修复也要走 PR 流程：

```bash
# 1. 创建修复分支
git checkout -b hotfix/critical-bug

# 2. 快速修复
git commit -m "fix: 修复关键bug"
git push origin hotfix/critical-bug

# 3. 创建 PR，标题标注 [URGENT]
# 4. 在团队群里通知，请求快速审核
# 5. 审核通过后立即合并
```

**为什么不能跳过？**
- 第二双眼睛可能发现更好的修复方式
- 避免修一个 bug 引入另一个 bug
- 保持流程一致性

### 场景 2: 自己的 PR 可以自己合并吗？

**不可以**，这违背了 Code Review 的初衷。

```
❌ 错误：自己创建 PR，自己审核，自己合并
✅ 正确：自己创建 PR，他人审核，审核通过后合并
```

GitHub 的分支保护规则可以配置"禁止自己批准自己的 PR"。

### 场景 3: PR 和 main 分支冲突了怎么办？

GitHub 会提示：

```
⚠️ This branch has conflicts that must be resolved
```

解决方法：

```bash
# 方法 1: Rebase (推荐)
git checkout feature/my-feature
git fetch origin
git rebase origin/main
# 解决冲突
git add .
git rebase --continue
git push -f origin feature/my-feature

# 方法 2: Merge
git checkout feature/my-feature
git merge origin/main
# 解决冲突
git add .
git commit -m "merge: 解决冲突"
git push origin feature/my-feature
```

### 场景 4: PR 审核卡住了，审核者没响应？

1. 在 PR 下友好提醒：`@reviewer 方便的时候帮忙看下这个 PR 😊`
2. 在团队群里询问
3. 如果审核者确实很忙，可以请另一个成员审核
4. 真正紧急的情况，联系项目负责人

### 场景 5: 审核意见不同意怎么办？

**开发者觉得审核意见不合理：**

1. 礼貌地解释你的设计思路
2. 讨论不同方案的优劣
3. 如果无法达成一致，请第三人参与讨论
4. 记住：Code Review 是讨论，不是命令

**示例对话：**

```
审核者：建议这里使用 useMemo 优化性能
开发者：感谢建议！我考虑过，但这个数据更新很频繁，
       useMemo 反而可能增加开销。我做了性能测试，
       现在的方案更快。测试数据：[链接]
审核者：哦！那确实现在的方案更好。Approved!
```

### 场景 6: 发现已合并的 PR 有问题？

```bash
# 不要 revert main 分支！
# 正确做法：

# 1. 创建新的修复 PR
git checkout -b fix/issue-from-pr-123
# 修复问题
git commit -m "fix: 修复 PR #123 引入的问题"
git push origin fix/issue-from-pr-123

# 2. 创建 PR，在描述中关联原 PR
# 3. 快速审核并合并
```

---

## 🎓 Code Review 最佳实践

### 审核者的心态

- ✅ 目标是改进代码，不是批评人
- ✅ 假设作者是经过思考的，询问而非命令
- ✅ 庆祝好的代码，不只是指出问题
- ✅ 明确区分必须修改和建议

### 审核清单

每次审核时检查：

#### 功能性
- [ ] 代码实现了预期功能吗？
- [ ] 边界情况处理了吗？
- [ ] 错误处理充分吗？

#### 可读性
- [ ] 代码容易理解吗？
- [ ] 变量和函数命名清晰吗？
- [ ] 需要添加注释吗？

#### 可维护性
- [ ] 代码容易修改和扩展吗？
- [ ] 有重复代码吗？
- [ ] 组件/函数职责单一吗？

#### 性能
- [ ] 有明显的性能问题吗？
- [ ] 不必要的重新渲染吗？
- [ ] 资源泄漏风险吗？

#### 安全性
- [ ] 输入验证充分吗？
- [ ] 敏感信息泄露吗？
- [ ] 权限检查正确吗？

---

## 💡 提示和技巧

### 对于开发者

1. **PR 保持小而专注**
   ```
   ✅ 好：一个 PR 完成一个功能
   ❌ 避免：一个 PR 改了 50 个文件，做了 10 件事
   ```

2. **写好 PR 描述**
   ```markdown
   ## 📝 变更说明
   添加导出功能，支持导出为 Excel 格式
   
   ## 🎯 实现方式
   使用 xlsx 库生成 Excel 文件
   
   ## ✅ 测试
   - 测试导出空数据
   - 测试导出大量数据 (1000+ 条)
   - 测试特殊字符处理
   ```

3. **主动请求具体的反馈**
   ```
   "这部分的性能优化我不太确定，能帮忙看下吗？"
   "这个组件拆分合理吗？"
   ```

### 对于审核者

1. **及时响应**
   - 尽量在 24 小时内回复
   - 太忙的话先说一声

2. **具体的反馈**
   ```
   ❌ "这里不好"
   ✅ "这里直接查询数据库可能慢，建议加个缓存"
   ```

3. **区分等级**
   ```
   🔴 阻止合并：这个 bug 必须修复
   🟡 强烈建议：这样会更好
   🟢 想法：也可以考虑这种方案
   ```

---

## 🛠️ 分支保护规则配置（给管理员）

如果需要修改保护规则：

1. 进入仓库 **Settings** → **Branches**
2. 点击 main 分支的 **Edit** 规则
3. 可配置选项：

```
[x] Require a pull request before merging
    [x] Require approvals (1)
    [x] Dismiss stale pull request approvals when new commits are pushed
    [ ] Require review from Code Owners

[ ] Require status checks to pass before merging
    (可以配置自动化测试)

[x] Require conversation resolution before merging
    (确保所有讨论都已解决)

[ ] Require signed commits
    (更高安全级别)

[x] Include administrators
    (规则对管理员也生效)
```

---

## 📞 获取帮助

如果对分支保护规则有疑问：

- 📖 查看 [GitHub 官方文档](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)
- 💬 联系项目负责人
- 🐛 提出 Issue 讨论

---

## 📚 相关文档

- [团队协作指南](./CONTRIBUTING.md) - 完整的协作流程
- [新成员入职指南](./新成员入职指南.md) - 快速上手
- [项目说明](./README.md) - 项目概览

---

**记住：这些规则是为了帮助我们，而不是限制我们。**

通过 Code Review 和分支保护：
- 🎯 提高代码质量
- 🤝 促进知识共享
- 🛡️ 保护主分支稳定
- 📈 提升团队技能

Happy reviewing! 🚀

