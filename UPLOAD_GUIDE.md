# 🎉 文件上传功能使用指南

## ✨ 新增功能

主页面现在支持**上传视频和Excel标注数据**，可以快速开始标注工作！

---

## 📝 使用步骤

### 1️⃣ 准备Excel文件

#### Excel格式要求：

**必须列**：
- `句子编号` / `编号` / `no` / `序号`
- `原文文本` / `原文` / `original` / `asr` / `识别结果`

**可选列**：
- `开始时间` / `start` / `起始` - 格式：`MM:SS` 或 `HH:MM:SS` 或数字（秒）
- `结束时间` / `end` / `终止` - 格式：`MM:SS` 或 `HH:MM:SS` 或数字（秒）
- `大模型改写文本` / `改写` / `rewrite` / `ai`

#### 示例Excel数据：

| 句子编号 | 开始时间 | 结束时间 | 原文文本 | 大模型改写文本 |
|---------|---------|---------|---------|---------------|
| 1 | 00:00 | 00:15 | 这是原文示例 | 这是改写示例 |
| 2 | 00:15 | 00:30 | 第二句原文 | 第二句改写 |
| 3 | 00:30 | 00:45 | 第三句原文 | 第三句改写 |

**提示**：点击"下载Excel模板"按钮可获取标准模板！

---

### 2️⃣ 上传文件

#### 操作步骤：

1. **打开上传弹窗**
   - 点击主页顶部的"上传视频和标注数据"按钮

2. **下载模板（可选）**
   - 如果不确定格式，先点击"下载Excel模板"
   - 按照模板格式填写数据

3. **上传视频文件**
   - 点击"选择视频文件"按钮
   - 支持格式：MP4、AVI、MOV等常见视频格式
   - 文件大小限制：500MB

4. **上传Excel文件**
   - 点击"选择Excel文件"按钮
   - 支持格式：`.xls` 或 `.xlsx`
   - 系统会自动识别列名（不区分大小写）

5. **开始标注**
   - 两个文件都上传后，"开始标注"按钮会激活
   - 点击按钮，系统自动解析数据并跳转到标注页面

---

### 3️⃣ 自动数据对齐

上传后，系统会自动：

✅ **解析Excel数据**
- 识别表头列名
- 提取句子编号、时间、文本等信息
- 支持多种列名写法（中英文均可）

✅ **创建视频预览**
- 生成视频本地预览URL
- 自动识别科目（如果文件名包含"数学"、"英语"等）

✅ **对齐到标注页面**
- 每条数据对应视频的时间段
- 原文和AI改写文本自动填充
- 点击时间可跳转到视频对应位置

---

## 🎬 标注页面功能

进入标注页面后，您可以：

### 视频播放
- 🎥 播放/暂停视频
- ⏱️ 点击时间范围自动跳转
- 🔄 重复播放当前片段

### 文本编辑
- ✏️ 编辑"人工标注文本"列
- 📝 修改AI改写的内容
- 💡 添加教研备注

### 问题分类
- 🏷️ 选择问题大类
- 🔖 选择问题小类
- ✅ 标记标注状态

### 提交保存
- 💾 点击"提交标注"保存数据
- 📊 查看已完成数量
- 🔙 返回主页

---

## 📋 Excel列名识别规则

系统会自动识别以下列名（不区分大小写）：

| 数据类型 | 可识别的列名 |
|---------|------------|
| **句子编号** | 句子编号、编号、no、序号 |
| **开始时间** | 开始时间、start、起始 |
| **结束时间** | 结束时间、end、终止 |
| **原文文本** | 原文、original、asr、识别结果 |
| **AI改写** | 改写、rewrite、ai、大模型 |

---

## 🔧 时间格式说明

支持以下时间格式：

1. **MM:SS** - 例如：`01:30`（1分30秒）
2. **HH:MM:SS** - 例如：`01:23:45`（1小时23分45秒）
3. **纯数字** - 例如：`90`（90秒）

系统会自动转换为秒数并显示为 `MM:SS` 格式。

---

## ⚠️ 常见问题

### Q1: 上传的视频不显示？
**A**: 请检查：
- 文件格式是否正确（MP4最佳）
- 文件是否损坏
- 浏览器是否支持该视频格式

### Q2: Excel解析失败？
**A**: 请确认：
- 第一行必须是表头
- 至少包含"句子编号"和"原文文本"列
- 数据行不能全为空
- 文件格式为`.xlsx`或`.xls`

### Q3: 时间跳转不准确？
**A**: 
- 检查Excel中的时间格式是否正确
- 确保时间格式统一（全部使用MM:SS或全部使用数字）

### Q4: 可以同时上传多个视频吗？
**A**: 目前每次上传一个视频和对应的Excel文件。如需标注多个视频，请分别上传。

### Q5: 上传的数据会保存吗？
**A**: 当前版本数据保存在浏览器本地。实际使用时需要对接后端API进行数据持久化。

---

## 💡 最佳实践

### 1. Excel文件准备
- 使用模板文件作为基础
- 保持列名简洁明了
- 时间格式统一

### 2. 视频文件
- 推荐使用MP4格式
- 压缩视频以加快上传速度
- 文件名包含科目信息（如"数学-01.mp4"）

### 3. 标注流程
- 先完整观看视频
- 按顺序标注，避免遗漏
- 及时保存标注结果

---

## 🚀 示例工作流程

```
1. 准备材料
   ↓
   - 录制/获取课程视频
   - 获取ASR识别结果
   - 获取大模型改写文本
   - 整理到Excel中

2. 上传文件
   ↓
   - 访问标注平台主页
   - 点击"上传视频和标注数据"
   - 上传视频文件
   - 上传Excel文件
   - 点击"开始标注"

3. 开始标注
   ↓
   - 播放视频查看内容
   - 编辑人工标注文本
   - 选择问题分类
   - 填写备注
   - 标记完成状态

4. 提交保存
   ↓
   - 检查标注完整性
   - 点击"提交标注"
   - 系统保存数据
   - 返回主页

5. 质检复检（可选）
   ↓
   - 抽样质检标注质量
   - PM复检问题分类
   - 导出分析报告
```

---

## 📦 导出功能

标注完成后，可以通过"结果分析"页面导出数据：

- 📊 问题分布统计
- 📈 可视化图表
- 📥 CSV格式导出
- 📋 包含所有标注信息

---

## 🎯 下一步

完成文件上传后，建议：

1. **熟悉界面** - 浏览标注页面各项功能
2. **测试跳转** - 点击时间范围测试视频跳转
3. **试标几条** - 先标注2-3条熟悉流程
4. **批量标注** - 正式开始完整标注工作

---

## 📞 技术支持

如遇问题，请查看：
- `README.md` - 项目说明
- `GUIDE.md` - 完整使用指南
- `START.md` - 快速开始

**祝您使用愉快！** 🎊

