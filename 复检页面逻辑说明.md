# 复检页面逻辑说明

## 标签页定义

### 待复检标签页
**筛选条件**：至少有一个标注人有待复检数据

- 显示：所有有待复检数据的视频
- 包括：
  - ✅ 完全待复检的视频（所有标注人都有待复检）
  - ✅ 部分复检完成的视频（有的标注人已完成，有的还没完成）

### 已复检标签页
**筛选条件**：`videos` 表中 `is_completed = true`

- 显示：所有点击过"完成复检"按钮的视频
- 说明：
  - ✅ 当某个视频的某个标注人的所有数据都复检完成时，点击"完成复检"按钮，会将该视频标记为已完成
  - ✅ 即使视频被标记为已完成，如果还有其他标注人的数据未复检，它仍会同时出现在"待复检"标签页

## 完成复检按钮的逻辑

在 `ReviewPage.tsx` 中，点击"完成复检"按钮时：

1. 更新该标注人的所有待复检数据，设置 `review_status = true` 和 `reviewer = 复检人姓名`
2. 检查该标注人的所有数据是否都已复检完成（`reviewData.every(item => item.status)`）
3. 如果是，则更新 `videos` 表：
   ```typescript
   {
     is_completed: true,
     review_completed_at: new Date().toISOString()
   }
   ```

## 重要说明

⚠️ **已复检标签页的视频可能仍有待复检数据**

例如，视频 A 有两个标注人：
- 标注人甲：完成了复检，点击了"完成复检"按钮
  - 此时 `videos.is_completed = true`
- 标注人乙：还有待复检数据

那么视频 A 会：
- ✅ 出现在"已复检"标签页（因为 `is_completed = true`）
- ✅ 同时出现在"待复检"标签页（因为标注人乙有待复检）

在"已复检"标签页中，视频 A 会显示所有标注人的信息：
- 标注人甲：`pendingCount = 0, reviewedCount > 0`（已完成）
- 标注人乙：`pendingCount > 0`（未完成）

## 数据统计

```
待复检标签页 = 至少有一个标注人有待复检数据的视频
已复检标签页 = is_completed = true 的视频

如果一个视频有多个标注人：
- 任何一个标注人点击"完成复检"按钮 → 进入"已复检"标签页
- 如果还有其他标注人有待复检数据 → 同时保留在"待复检"标签页
```

## 标注人级别统计

每个视频卡片显示的标注人信息：

- **待复检标签页**：只显示有待复检数据的标注人
- **已复检标签页**：显示所有标注人的信息（不管是否完成复检）
  - 这样可以看到哪些标注人已完成，哪些还未完成

每个标注人的统计包括：
- `totalAnnotations`: 该标注人在这个视频中的总标注数
- `reviewedCount`: 已复检的数量
- `pendingCount`: 待复检的数量
- `unannotatedCount`: 未标注的数量
- `reviewers`: 复检人列表
- `inspectors`: 抽检人列表
- `lastReviewTime`: 最新复检时间

## 复检完成的定义

**标注人级别**：
- 该标注人的所有数据都已复检（`reviewData.every(item => item.status)`）
- 点击"完成复检"按钮

**视频级别**：
- 至少有一个标注人完成了复检并点击了"完成复检"按钮
- `videos.is_completed = true`

## 排序规则

- **待复检标签页**：按最早待复检时间排序（最早的在前）
- **已复检标签页**：按复检完成时间（`review_completed_at`）排序（最新的在前）

## 使用场景

### 待复检标签页
- 查看还有哪些工作需要做
- 选择视频进行复检
- 按标注人筛选待复检任务

### 已复检标签页
- 查看已完成的工作
- 按复检完成时间排序，方便回顾
- 查看每个视频的所有标注人的复检进度
- 识别哪些视频还有标注人未完成复检

## 注意事项

1. **点击"完成复检"按钮的时机**：
   - 只有当某个标注人的所有数据都复检完成时，才能点击
   - 一旦点击，视频会立即进入"已复检"标签页

2. **视频在两个标签页都出现**：
   - 这是正常的！说明有的标注人完成了，有的还没完成
   - 只有当所有标注人都没有待复检数据时，视频才会从"待复检"标签页消失

3. **is_completed 字段的含义**：
   - `is_completed = true`：至少有一个标注人完成了复检
   - 不代表所有标注人都完成了复检
   - 如果需要知道是否所有标注人都完成，需要检查每个标注人的 `pendingCount`
